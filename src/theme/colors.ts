const global = {
  primary: '#383838',
};

const base = {
  black: '#000000',
  white: '#ffffff',
  red: '#ff0000',
  green: '#009000',
  blue: '#0000ff',
};

const colors = {
  global,

  base,
};

export default colors;
